# [issues/issue_154.md] - Issue #154 discussion

**Type:** review
**Keywords:** performance problems, vsync enabled, 10 fps, lag spikes, low CPU usage, low GPU usage, hardware testing
**Concepts:** performance, vsync, frame rate

## Summary
The issue reports performance problems with vsync enabled (10 fps) and disabled (lag spikes) while CPU and GPU usage are low.

## Explanation
The user is experiencing significant performance issues in the game, specifically noting that enabling vsync results in a capped frame rate of 10 fps, which makes the game unplayable. When vsync is disabled, there are consistent lag spikes despite low CPU and GPU usage. The user offers to provide hardware for testing on different systems.

## Related Questions
- What is the expected behavior of vsync in this game?
- Are there any known issues with vsync on Windows systems?
- How does the game handle frame rate capping when vsync is enabled?
- Can low CPU and GPU usage still cause lag spikes in the game?
- Is there a way to diagnose performance issues without changing hardware?
- What are the potential causes of inconsistent frame rates with vsync disabled?

*Source: unknown | chunk_id: github_issue_154_discussion*
