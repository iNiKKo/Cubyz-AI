# [issues/issue_875.md] - Issue #875 discussion

**Type:** review
**Keywords:** Stack Size, Item Declaration, Configuration, Parameter, Block
**Symbols:** .stackSize, .item
**Concepts:** Item Configuration, Parameter Usage

## Summary
The issue discusses setting a stack size for items, which is already supported through the `.stackSize` parameter in the item declaration.

## Explanation
The discussion clarifies that the stack size of an item can be set using the `.stackSize` parameter within the block's item declaration. The maintainer emphasizes that this parameter applies to items, not blocks, and provides an example of how to correctly use it in the code.

## Related Questions
- How is the stack size of an item set in Cubyz?
- What parameter should be used to specify the stack size for items?
- Can the stack size be different for different items?
- Where should the `.stackSize` parameter be placed in the code?
- Is there a default stack size if none is specified?
- How does the stack size affect item handling in Cubyz?

*Source: unknown | chunk_id: github_issue_875_discussion*
