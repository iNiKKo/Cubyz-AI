# [easy/codebase_assets_cubyz_blocks_limestone_pillar.zig.zon] - Chunk 0

**Type:** configuration
**Keywords:** configuration, block properties, texture assignment, model type, rotation behavior
**Concepts:** block configuration, texture mapping

## Summary
Defines configuration for a limestone pillar block in Cubyz.

## Explanation
This chunk is a configuration file defining properties of a limestone pillar block. It specifies the rotation behavior, model type, and textures used for different sides of the block. The `.rotation` field indicates that the block can rotate in all directions. The `.model` field specifies that the block uses a cube model. The `.texture`, `.texture_top`, and `.texture_bottom` fields define the textures applied to the main sides and top/bottom of the pillar, respectively.

## Related Questions
- What is the rotation behavior of the limestone pillar block?
- Which model type does the limestone pillar use?
- What texture is applied to the main sides of the limestone pillar?
- How are the top and bottom textures of the limestone pillar defined?
- Is there any special configuration for the rotation of this block?
- What is the primary texture used for the limestone pillar?

*Source: unknown | chunk_id: codebase_assets_cubyz_blocks_limestone_pillar.zig.zon_chunk_0*
