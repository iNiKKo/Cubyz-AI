# [hard/codebase_src_items.zig] - Chunk 7

**Type:** configuration
**Keywords:** enumeration, item types, category system, base items, procedural generation
**Symbols:** ItemType, ItemType.baseItem, ItemType.proceduralItem, ItemType.null
**Concepts:** item management, categorization

## Summary
Defines an enumeration for item types in the game.

## Explanation
This chunk defines an enumeration named `ItemType` with three variants: `baseItem`, `proceduralItem`, and `null`. Each variant is associated with a unique unsigned 7-bit integer value. This enumeration is likely used to categorize different types of items within the game, such as base items that are predefined and procedural items that are generated dynamically.

## Related Questions
- What are the possible values for an item type in the game?
- How many different item types are defined in this chunk?
- What does `baseItem` represent in the context of item types?
- Can you list all the variants of the `ItemType` enumeration?
- Is there a variant in the `ItemType` enum that represents no item?
- What is the underlying data type for each variant in the `ItemType` enum?

*Source: unknown | chunk_id: codebase_src_items.zig_chunk_7*
