# [easy/codebase_assets_cubyz_blocks_terracotta_pillar.zig.zon] - Chunk 0

**Type:** configuration
**Keywords:** configuration, block properties, textures, rotation, model
**Concepts:** block configuration, texturing

## Summary
Defines configuration for a terracotta pillar block in Cubyz.

## Explanation
This chunk is a configuration file defining the properties of a terracotta pillar block. It specifies the rotation behavior, model type, and texture paths for different sides of the block. The `.rotation` field indicates that the block can rotate based on the 'cubyz:direction' property. The `.model` field sets the block's model to 'cubyz:cube', suggesting a simple cubic shape. The `.texture`, `.texture_top`, and `.texture_bottom` fields define the textures for the sides, top, and bottom of the block, respectively.

## Related Questions
- What is the rotation behavior of the terracotta pillar block?
- Which model type is used for the terracotta pillar block?
- What texture is applied to the sides of the terracotta pillar block?
- Which texture is used for the top of the terracotta pillar block?
- Which texture is used for the bottom of the terracotta pillar block?
- How many different textures are defined for the terracotta pillar block?

*Source: unknown | chunk_id: codebase_assets_cubyz_blocks_terracotta_pillar.zig.zon_chunk_0*
