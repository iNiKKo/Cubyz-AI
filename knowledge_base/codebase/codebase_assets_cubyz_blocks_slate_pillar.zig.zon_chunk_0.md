# [easy/codebase_assets_cubyz_blocks_slate_pillar.zig.zon] - Chunk 0

**Type:** configuration
**Keywords:** block properties, texture mapping, model definition, rotation behavior, configuration data
**Concepts:** block configuration

## Summary
Defines configuration for the Slate Pillar block in Cubyz.

## Explanation
This chunk contains a single anonymous struct that defines the configuration properties of the Slate Pillar block. It specifies the rotation behavior, model type, and texture paths for different sides of the block. The `.rotation` field indicates that the block can be rotated in various directions as defined by the 'cubyz:direction' property. The `.model` field sets the block's model to a cube. The `.texture`, `.texture_top`, and `.texture_bottom` fields define the textures for the sides, top, and bottom of the block, respectively.

## Related Questions
- What is the rotation property of the Slate Pillar block?
- Which model type is used for the Slate Pillar block?
- What texture is applied to the sides of the Slate Pillar block?
- What texture is applied to the top of the Slate Pillar block?
- What texture is applied to the bottom of the Slate Pillar block?
- How many different textures are defined for the Slate Pillar block?

*Source: unknown | chunk_id: codebase_assets_cubyz_blocks_slate_pillar.zig.zon_chunk_0*
