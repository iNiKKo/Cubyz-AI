# [easy/codebase_src_proceduralItem_modifiers_single_use.zig] - Chunk 0

**Type:** implementation
**Keywords:** packed struct, configuration loading, modifier combination, property setting, tooltip printing
**Symbols:** Data, priority, loadData, combineModifiers, changeProceduralItemParameters, printTooltip
**Concepts:** procedural item modifiers, strength property, maximum durability, tooltip generation

## Summary
Defines a single-use procedural item modifier with strength property.

## Explanation
This chunk defines a single-use procedural item modifier that affects an item's maximum durability. It includes a packed struct `Data` to store the strength value, a function `loadData` to initialize this data from a configuration file, a function `combineModifiers` to combine two modifiers by taking the minimum of their strengths, a function `changeProceduralItemParameters` to apply the modifier's strength to an item's maximum durability, and a function `printTooltip` to generate a tooltip string describing the modifier.

## Code Example
```zig
pub fn loadData(zon: main.ZonElement) Data {
	return .{.strength = @max(1, zon.get(f32, "strength") orelse 1)};
}
```

## Related Questions
- What is the purpose of the `Data` struct?
- How does the `loadData` function initialize the modifier data?
- What operation does the `combineModifiers` function perform on two modifiers?
- How is the item's maximum durability affected by the modifier?
- What string is generated by the `printTooltip` function?
- What is the priority value of this modifier?

*Source: unknown | chunk_id: codebase_src_proceduralItem_modifiers_single_use.zig_chunk_0*
