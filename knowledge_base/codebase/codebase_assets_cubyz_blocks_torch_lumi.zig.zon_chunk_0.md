# [easy/codebase_assets_cubyz_blocks_torch_lumi.zig.zon] - Chunk 0

**Type:** implementation
**Keywords:** torch block, light emission, collision detection, update function, modeling, texture mapping, LOD replacement
**Symbols:** torch, blockHealth, drops, replaceable, emittedLight, viewThrough, absorbedLight, collide, rotation, onUpdate, model, texture, item, lodReplacement
**Concepts:** block properties, light emission, collision detection, update function, modeling, texture mapping, LOD replacement

## Summary
Defines a torch block with specific properties and behaviors.

## Explanation
This chunk defines a torch block in the Cubyz voxel engine. It specifies various properties such as health, drops, replaceability, emitted light, view-through status, absorbed light, collision behavior, rotation, update function, model, texture, item texture, and LOD replacement. The block is designed to emit light and can be placed on blocks that support it.

## Related Questions
- What is the health of the torch block?
- Does the torch block drop items automatically?
- Is the torch block replaceable?
- What light color does the torch emit?
- Can the torch be viewed through blocks it is placed on?
- How much light does the torch absorb?
- Does the torch collide with other blocks?
- What rotation does the torch have?
- What function is called when the torch block updates?
- Which model is used for the torch block?
- What texture is used for the torch block's base and side faces?
- What item texture is used for the torch block in-game inventory?

*Source: unknown | chunk_id: codebase_assets_cubyz_blocks_torch_lumi.zig.zon_chunk_0*
