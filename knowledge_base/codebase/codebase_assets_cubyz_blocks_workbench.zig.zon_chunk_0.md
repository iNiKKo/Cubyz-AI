# [easy/codebase_assets_cubyz_blocks_workbench.zig.zon] - Chunk 0

**Type:** implementation
**Keywords:** choppable, health, drops, interact, window, model, rotation, texture
**Symbols:** workbench
**Concepts:** block, interaction, texture

## Summary
Defines a workbench block with health, drops, interaction behavior, and textures.

## Explanation
The chunk defines a workbench block with the following properties:
- `tags`: Contains the tag `.choppable`.
- `blockHealth`: Sets the block's health to 10.
- `drops`: Specifies that the block drops an item automatically.
- `onInteract`: Defines interaction behavior, opening a window named 'workbench'.
- `model`, `rotation`, and texture fields define the visual appearance of the workbench.

## Related Questions
- What is the health of the workbench block?
- How does the workbench interact with players?
- What textures are used for the workbench?
- What tag does the workbench have?
- What items can be dropped by the workbench?
- What model is used for the workbench?
- What rotation is applied to the workbench?
- What texture is used on the front of the workbench?
- What texture is used on the back of the workbench?
- What texture is used on the left side of the workbench?
- What texture is used on the right side of the workbench?
- What texture is used on the top of the workbench?

*Source: unknown | chunk_id: codebase_assets_cubyz_blocks_workbench.zig.zon_chunk_0*
